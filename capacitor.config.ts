import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rohan.timeline',
  appName: 'World History Timeline',
  webDir: 'dist'
};

export default config;
